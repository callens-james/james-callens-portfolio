# Install Strategy

## Profiles
1. Core Only (default)
2. Core + Local AI (optional)
3. Core + OpenClaw Integration (optional)

## Rules
- No optional component auto-installed
- User must explicitly opt in
- Show performance/security warning before enabling optional integrations
- Allow changing profile later in Settings
