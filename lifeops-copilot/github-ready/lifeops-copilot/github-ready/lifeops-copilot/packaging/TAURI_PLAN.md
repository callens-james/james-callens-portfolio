# Tauri Packaging Plan
- Target installers: Windows (.msi/.exe), macOS (.dmg), Linux (.AppImage/.deb)
- Core-only default profile
- Optional integrations toggled post-install in Settings
- Include installer warnings for performance/security before enabling optional modes
